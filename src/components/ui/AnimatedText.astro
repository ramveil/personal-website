---
interface Props { class?: string; content: string | Promise<string>; duration?: number; delay?: number; stagger?: number; }
const { content, class: className } = Astro.props;
---
<span class:list={['animated-text', className]}>{await content}</span>
