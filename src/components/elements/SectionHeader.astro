---
import { FolderOpen } from '@lucide/astro';
const { title, description } = Astro.props;
---
<div class="section-heading"><h2><FolderOpen size={27} stroke-width={1} aria-hidden="true" />{title}</h2>{description && <p>{description}</p>}</div>
